# ═══════════════════════════════════════════════════════════
# requirements.txt — 顶层依赖声明（手动维护源文件）
# 全量锁定版本见 requirements.lock（自动生成，勿编辑）
# 更新 lock: pip freeze > requirements.lock
# ═══════════════════════════════════════════════════════════

# 核心依赖
pandas>=2.0
numpy>=1.24
requests>=2.28
scipy>=1.10

# 数据源
akshare>=1.14
baostock>=0.8
tushare>=1.4

# 可选数据源（增强容灾）
yquoter>=0.4.2
adata>=2.9.5

# 自然语言选股
pywencai>=0.1

# 报告生成
python-docx>=1.1
openpyxl>=3.0
matplotlib>=3.7

# 机器学习 (AI预测)
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0

# 工具
python-dotenv>=1.0

# 数据库异步化
aiosqlite>=0.20

# Web API (Tauri 桌面端后端)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
python-multipart>=0.0.9
pyarrow>=10.0
