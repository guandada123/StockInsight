# 2026-08-24 工作日志

## 18:50 StockInsight 收盘选股周日 hang 处置（用户转发 L3 告警 automation-1784555575881）
- 根因：周日非交易日 5 K 线源全离线 → `fetch_kline` 无单源硬超时(仅 300s 熔断冷却) → 单只 analyze_one 阻塞超 `future.result(45s)` → 数百只×8线程全卡 → 墙钟超 600s 外层 timeout 杀(exit 124)。**非 future 死锁**，告警"提示3"猜测不成立。
- 自愈：08-24 周一 15:33 重跑，daily_scores=123 行(top 000703=74.0) → 已恢复。
- 修复：cli.py cmd_scan 顶部加非交易日短路(weekday>=5 → return 0)。ast OK + 逻辑验证通过。交易日零改动。
- 止血：cli.py cmd_scan 顶部加非交易日短路(weekday>=5 → return 0)。ast OK + 逻辑验证通过。
- 19:00 根治项「单源硬超时」已落地(用户授权"可以")：data_sources.py 加 PER_SOURCE_TIMEOUT=8s + 进程级 _SRC_TIMEOUT_POOL(max_workers=8)，包裹 DataSourceChain.fetch_kline 每源。验证 success 0.00s 无回归 / 单源 hang 8.0s 跳源 / 5源全 hang 最坏 24.0s(<45s 外层窗)。初版 max_workers=1 串行化坑已修(改8)。
- 19:01 根治项「交易日历精确化」已落地(用户指令"接 tushare_loader 交易日表")：新增 stock_analyzer/trade_day.py，is_trading_day(dt) 优先查 stock_cache.db.stock_trade_calendar(tushare_loader schema)，表缺失/越界 fallback 内置 2026 休市(上交所 2025-12-22 官方核实)+weekday<5(保守不误跳)。cli.py cmd_scan 短路升级为 `if not is_trading_day(now.date()): return 0`。
- 部署：容器跑旧镜像，本地改动 docker cp 进 stockinsight-api-1 生效；stock_cache.db 是 bind mount。本地生成 2026 全年交易日历(365行/242交易日)写入 stock_trade_calendar，容器经 mount 读到 → 表优先路径生效。越界自动 fallback。容器内验证 is_trading_day 表优先正确(08-24→交易日/10-01→非交易日/2027-01-01→None)。
- 经验：容器代码与本地不同步(Dockerfile COPY 构建时固化)，改运行时文件须 docker cp 或重建镜像才生效；stock_cache.db 是实时 mount 故 DB 改动立即对容器可见。

## 19:25 排查补充(工程类)
- 误判纠正: 原以为容器跑旧镜像需重建固化, 实际 docker-compose.yml 第12行 `.:/app:ro` 已把本地目录只读挂载覆盖镜像 `/app`, 运行时代码以本地为准(非镜像COPY层)。故 git commit 的修复经 mount 实时对容器可见, 重启容器也用本地最新代码, **无需重建镜像**。
- 镜像构建失败(docker compose build 卡前端 `npx vite build` exit1)不影响运行时(mount优先), 是独立前端技术债, 与本次Python修复解耦。
- 已清冗余 docker commit 镜像 fixed-20260824(多余)。容器仍 Up 4d healthy, is_trading_day 验证正确。
- 其余待处理(低优先): ML测试5失败=本地venv缺xgboost(容器/CI有); ruff I001×2预存项目风格不动; 自动化1781682224691开盘前自检PAUSED待定; 交易日表2027越界fallback明年初补; compose version弃用警告可删。
