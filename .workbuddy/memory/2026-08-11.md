# 2026-08-11 工作日志

## StockInsight 收盘选股扫描 — 异常（K线数据源中断）
- 调度 automation-1784555575881（15:26）触发，L3 前置护栏判定交易日✅。
- 容器内 `cli.py scan --mode mainboard --top-n 20` 卡死：首轮运行 21min 无 `结果已保存`；重跑后速率 83→3.4 只/s 坍塌，失败计数 1464+。
- 根因：**4 个 K 线数据源(Sina/Tencent/AData/Tushare)同时熔断 + yquoter RemoteProtocolError 持续重试**，指向 stockinsight-api-1 容器出网到行情主机持续中断；实时行情快速过滤阶段正常。
- 处置：两次均主动 SIGKILL 终止（容器内/本机无残留），按「未能完成=真异常」判定推送飞书告警到 oc_9ee5303497f5e0e71666b610d6bdc346（msg om_x100b68992c1c40a4b32232159d42f6f，送达）。
- 与 08-07 区别：08-07 是扫描完成但日志有抖动（误报）；本次是交易日真实失败（源持续中断），告警合理。
- 待跟进：行情源恢复后手动重跑确认；长期建议排查容器出网连通性 / 增加 K 线源降级兜底。
