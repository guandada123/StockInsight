# 2026-07-21 StockInsight 收盘选股扫描崩溃修复

## 故障
- 时间 15:25 收盘扫描 cmd_scan 排名/展示阶段 ImportError: `cannot import name 'get_stock_sector' from 'stock_analyzer.sector_info'`
- 进度: 3191→过滤2416→扫描133只达标, 崩溃在排名展示(cli.py:685)
- 报错提示"正确函数名应为 get_stock_all_sectors" — **误报**

## 根因
- `sector_info.py` 函数重命名 `get_stock_sector → get_stock_sector_full` 时, `cli.py:685` 这处漏改(全项目其他 10+ 处均用 `_full`)
- `get_stock_all_sectors` 返回 **dict**, `cli.py:687` 的 `[:10]` 切片会崩 → 不是正确名
- 正确名 `get_stock_sector_full` 返回 **str**("制造业 > 半导体"), 适配 `[:10]` 短名显示

## 修复（commit d21a81e）
- cli.py:685 `import get_stock_sector` → `import get_stock_sector_full`
- cli.py:687 `get_stock_sector(...)` → `get_stock_sector_full(...)`
- 验证: py_compile OK; 全局无裸 `get_stock_sector` 残留; 冒烟导入+调用返回 str

## 验收建议
- 重跑当日收盘扫描(cmd_scan 对应模式), 确认 133 只达标结果能正常排名+展示+保存
- 注意: 崩溃在展示前(no_save 分支未触发), 当日数据需重跑生成

## 17:58 实跑验证通过
- 命令: `python cli.py scan --mode mainboard --no-save --min-score 0`
- 结果: 扫描完成 2414 只达标(因 min-score=0), 跳过/失败 0, 耗时 1min
- 排名展示阶段(cli.py:685 修复点)正常执行, `get_stock_sector_full(code)[:10]` 切片不崩
- 板块列显示"未知": 沙箱本地 sector DB 无数据(get_stock_sector_full 返回默认"未知"占位), 非 bug; 有板块数据的机器正常显示
- 结论: ImportError 修复确认有效, 收盘扫描排名展示链路恢复
